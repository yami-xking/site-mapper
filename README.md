# site-mapper
used to map all the site linked to given url or redirecting urls

# Clone repository
git clone https://github.com/yourusername/yourrepo.git
cd yourrepo

# Install dependencies
pip install -r requirements.txt

# Run the tool
python site-mapper.py
